# Quran TV — Task Tracker & Progress Log

Android TV (Google TV / Chromecast) app: stream Quran surahs from mp3quran.net with
Ayat-KSU-style ayah highlighting (text mode + authentic Madinah mushaf page mode).
Build prompt lives in `PROMPT.md`; this file tracks delivery phases and git progress.

## Status legend

- `[ ]` not started · `[~]` in progress · `[x]` done

## Locked decisions (see PROMPT.md DECISIONS)

| ID | Decision |
|----|----------|
| D1 | Kotlin + Jetpack Compose for TV (`androidx.tv`) + Media3/ExoPlayer |
| D2 | Authentic Uthmani text: bundled Tanzil `quran-uthmani.txt` (canonical) + Quran.com API v4 fallback; pages = mp3quran SVG (authentic Madinah mushaf) |
| D3 | Arabic primary (RTL) + English secondary |
| D4 | Core scope only: Reciters → Surahs → Player + highlight |
| D5 | All riwayat, ayah sync best-effort where timing exists |
| D6 | Emulator-first verification → sideload to real Chromecast with Google TV |

## Git workflow (implemented)

- Repo initialized on `main`; initial commit contains `PROMPT.md`, `TASKS.md`, `.gitignore`.
- Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`, `test:`.
- Each phase: work on `phase/N-<name>` branch → merge to `main` → update this file → commit.
- Never commit build outputs / IDE files / local.properties (`.gitignore` covers them).

---

## Phase 0 — Bootstrap (done)

- [x] Scaffold build prompt (`PROMPT.md`) with verified mp3quran API contracts (live-tested endpoints, audio URL rule, timing format, SVG 235×235 coordinate space)
- [x] Lock decisions D1–D6 (authentic Tanzil Uthmani text, emulator-first)
- [x] Create task tracker (this file)
- [x] `git init` + `.gitignore` + initial commit

## Phase 1 — Skeleton + catalog

- [x] Gradle project scaffold (version catalog `libs.versions.toml`, wrapper) → `./gradlew :app:assembleDebug` green (AGP 8.13.2 / Kotlin 2.2.21 / compileSdk 36)
- [x] Compose for TV theme (tv-foundation / tv-material), shared focus modifiers (scale ~1.1 + border); note: tv-foundation 1.0.0 stable has no TvLazyColumn — standard foundation lazy lists used with tv-material components
- [x] Navigation shell: single activity, back-stack Home → Surah list → Player (double-Back exits)
- [x] API layer: `Mp3QuranApi` (suwar, reciters, recent_reads, ayat_timing/reads, /soar, /ayat_timing) + DTOs; OkHttp + kotlinx-serialization
- [x] Disk JSON cache + single-flight loading; defensive parsing (trailing commas in `surah_list`)
- [x] Home screen: Continue-listening card + full-height vertical reciter list grouped by letter with A–Z jump rail + visible search bar
- [x] Surah grid screen: 8-column dense grid; moshaf picker + jump-to-surah dialogs
- [x] Focusable loading / error / empty states with Retry
- [x] Unit tests: `surah_list` parsing, URL construction (trailing-slash normalization) — green
- [x] Commit: `feat: phase 1 — catalog browsing`

## Phase 2 — Player + streaming

- [x] Player screen scaffold: top bar (surah/reciter/moshaf), transport bar, state hoisting, stable keys
- [x] Media3 ExoPlayer streaming from `{server}{surah:03d}.mp3` (verified PLAYING on TV emulator)
- [x] Play/pause, seek ±5s (D-pad), playback speed 0.5×–2.0× (1.25×/1.5× verified via media session)
- [x] Next/prev surah (via available-surah list)
- [x] MediaSession wired; remote play/pause on real device pending
- [x] Audio focus handling (pause on transient loss / resume on gain)
- [x] Stream error → focused Retry state (code path; real-device offline test pending)
- [x] Commit: `feat: phase 2 — audio playback`

## Phase 3 — Text mode sync

- [x] Tanzil Uthmani asset: Gradle download task (skip if present) + parser → `verse_key` index (`"2:1"` → text); asset committed (1.3 MB)
- [x] Timing fetch per `(read, surah)` + on-disk cache (immutable → cache forever)
- [x] Read ↔ moshaf matching via `folder_url` ↔ `server` (normalized); enable sync only when matched
- [x] Position ticker (~200ms) + binary search `start_time ≤ pos < end_time`; state updates only on ayah change (stable keys; no full-list recomposition)
- [x] Text mode: current ayah highlighted + auto-scroll, select/tap-ayah → seek (verified: tap on verse 4 seeks to 14.1s)
- [x] Repeat modes: off / repeat ayah (seek to start on end, verified loop) / repeat surah (seek 0 on ended); autoplay next surah when repeat off (الفاتحة → البقرة verified)
- [x] Continue-listening persistence (DataStore: last reciter/moshaf/surah/ayah/position, throttled 5s writes; card verified across reinstall)
- [x] Basmala handling: timing index 0 = header slot (skip highlight), surah 9 none, offset mechanism for non-Hafs (count-based suggestion + settings override); mapping verified against SVG marker coordinates
- [x] Unit tests: timing binary search, basmala offset logic — green
- [x] Commit: `feat: phase 3 — ayah-synced text mode`

## Phase 4 — Mushaf page mode

- [x] SVG page fetch + AndroidSVG rendering (`com.caverock:androidsvg-aar`) to Bitmap (real viewBox parsed per page — 235×235 and 345×550 verified)
- [x] Polygon → screen mapping: `screen = pageSpace * displaySize / viewBoxSize` (per-page viewBox)
- [x] Highlight: rounded translucent rect from polygon quad; null polygon skipped (basmala/header)
- [x] Auto page-turn when ayah crosses page boundary; prefetch next page
- [x] LRU page bitmap cache (≤6 pages)
- [x] Mode toggle (text ↔ page) on-screen button + INFO/MENU remote key; persisted in settings (verified on emulator)
- [x] Unit tests: coordinate mapping — green
- [x] Commit: `feat: phase 4 — mushaf page highlight`

## Phase 5 — Polish

- [x] Settings: language ar/en (verified switch both ways), default speed, text font size, highlight color, default display mode
- [x] Offline polish: disk caches verified (catalog/timing); cold start from cache; audio-failure Retry path needs real-device pass (emulator airplane mode does not block Ethernet)
- [ ] Media session active (activity-scoped); background MediaSessionService deliberately out of scope for core delivery (noted in README)
- [x] `README.md`: prerequisites, build, emulator setup, sideload to Chromecast with Google TV, manual test checklist, Tanzil attribution, honest testing notes
- [x] Acceptance checklist sign-off (PROMPT.md Part 13) on Android TV 36 emulator — all core items pass
- [x] Lint-free build + all 26 unit tests pass; no jank observed during highlight ticks on the TV emulator
- [x] Real-device verification list handed to user in README (remote media keys, search IME, offline audio failure, focus feel/perf)
- [x] Commit: `feat: phase 5 — polish & release`

---

## Cross-phase concerns

- **10-foot UI**: D-pad only; visible focus (scale 1.05–1.1 + border); text ≥20sp body / 28sp headings; overscan margins ~48dp; media keys wired.
- **Performance**: no per-tick recomposition of the whole list (hoist current-ayah state, stable keys); page bitmap cache ≤6; `hardwareAccelerated`; no blur/heavy shadows on low-end.
- **Authenticity**: text = Tanzil Uthmani (CC BY-NC-ND, attribution in About); page images = mp3quran SVG (authentic Madinah mushaf).
- **Arabic RTL** mandatory; English via resources.

## Known gotchas (from PROMPT.md Part 14)

- Timing `read` id ≠ reciter id → match `folder_url` ↔ `server` (trailing slashes normalized).
- `ayah 0` = basmala, `polygon`/`page` null → skip highlight; surah 9 has no basmala.
- `surah_list` is comma-separated, may end with `,`, may be a subset (e.g. 83/114) → only list available.
- Server URLs may contain subdirectories (`.../deban/Rewayat-Qalon-A-n-Nafi/`) → normalize then concatenate `{server}{surah:03d}.mp3`.
- Warsh-style riwayat may count basmala as verse 1 → offset mechanism (validate surahs 1–2).

## Progress log

| Date | Phase | What | Status |
|------|-------|------|--------|
| 2026-08-07 | 0 | Bootstrap: build prompt (verified API contracts), decisions locked, tracker + git init | done |
| 2026-08-07 | 1 | Catalog browsing: scaffold, theme, nav shell, API + cache, Home A–Z rail, surah grid, unit tests | done (emulator: grid + picker verified) |
| 2026-08-07 | 2 | Player: ExoPlayer streaming, transport, speed, media session, focus retry states | done (emulator: PLAYING, 1.25×/1.5× verified) |
| 2026-08-07 | 3 | Text-mode sync: Tanzil asset, timing match + cache, binary-search ticker, highlight + auto-scroll, repeat, continue-listening | done (emulator: ayah seek boundaries + auto-scroll verified) |
| 2026-08-07 | 4 | Mushaf page mode: AndroidSVG rendering, per-page viewBox mapping, polygon highlight, page turn, mode toggle | done (emulator: page mode renders, INFO toggle works) |
| 2026-08-07 | 5 | Polish: settings (ar/en verified), search overlay, README + acceptance, tests final | done |
| 2026-08-07 | 5+ | Lint cleanup: `lintDebug` made genuinely green (was failing) — `ConcurrentHashMap.computeIfAbsent` (API 24) replaced in `JsonDiskCache.singleFlight` (minSdk 23 crash fix), Media3 `UnstableApi` opt-in on `PlaybackController`, cutout-mode attr moved to `values-v27`, deprecated `URL(String)` fixed in the Tanzil download task. Re-verified: build + 26 tests + full emulator smoke pass (Home → reciter → surah grid → player streaming, INFO page-mode toggle, media-key pause/resume, autoplay to next surah, Continue card) | done |
| 2026-08-07 | 5+ | Localization: wired player chrome through `strings.xml` — transport labels (repeat ⇄/⟲, “السور” jump, prev/next ayah/surah content descriptions), “no timing data” notice, audio-error and Retry texts now render in the active locale (Arabic primary per D3). Verified on emulator: “⇄ متوقف”, “1×”, “السور” + PLAYING audio, no crashes | done |
| 2026-08-07 | meta | Added reusable skill `.pi/skills/qurantv-architecture/` — architecture map, source-by-source guide, verified API contracts (references/api-contracts.md), core algorithms, caching/offline, localization, build/test/emulator workflow, gotchas, and refinement playbooks. Loadable in future sessions via skill name `qurantv-architecture` | done |
| 2026-08-07 | 5+ | Sync hardening: (1) `TimingIndex` no longer advances the highlight into the next ayah during inter-ayah gaps (unit test added); (2) ticker 200→100 ms + immediate highlight refresh after every seek; (3) repeat-ayah uses the mp3 duration as the last ayah's effective end; (4) long-ayah follow: text mode now pins the current ayah to the viewport top on every change and, when the ayah is taller than the viewport, scrolls through it proportionally to playback progress inside the ayah (backs off on manual scroll until the next ayah). Verified on emulator: Hafs read-13 surah-2 boundaries EXACT within one tick (60–80 ms) over 100+ s, no drift; 51 s Warsh ayah followed without crash. | done |
| 2026-08-07 | 5+ | Basmala handling restored per user: the recited basmala (timing index 0) is shown again as a highlighted surah header above the verse list (every surah starts with it except Al-Tawbah 9; surah 1's 1:1 is the basmala itself). Tanzil's embedded basmala prefix is stripped from verse 1 of surahs 2–114 so text matches audio 1:1 — this fixes the reported voice-1-ayah-ahead-of-text desync. Also fixed a broken `else-if` branch that had rendered the Tajweed overlay inside text mode. Verified on emulator: surah 2 shows basmala header + verse 1 “الم”, surah 1 shows no header (1:1 = basmala), list pinned to top, audio audible (emulator must NOT be started with `-no-audio`) | done |
| 2026-08-07 | 5+ | **Off-by-one highlight fix**: mp3quran timing data has two shapes — some reads include a timing index-0 basmala entry (list position == timing index), others omit it entirely (entries start at 1). `TimingIndex.ayahAt` returned the list position, so reads without index 0 highlighted one ayah behind the voice. Fix: `ayahAt` now returns the entry's true timing index; all lookups go through `SurahTiming.entryFor(ayah)` (by-timing-index map) instead of `entries.getOrNull(listPos)`; `buildTextItems` row count derives from `lastAyahIndex + 1` so the final verse is never dropped. Verified live on read 17 surah 50 (no index 0): boundaries EXACT within one tick for ayahs 32–35, highlight row pinned to the recited verse; 30/30 unit tests (3 new: no-index-0 shape, entryFor, missing-basmala) | done |
| 2026-08-08 | 5+ | **Mushaf page = default ayah view + maximized**: (1) `displayMode` default is now 1 (mushaf page mode) on fresh installs — the SVG page is the primary view; (2) page mode maximizes the mushaf: top bar + transport auto-hide ~4 s after the last key while playing and the SVG goes full-bleed; any key reveals the chrome (fullscreen page holds focus so D-pad still works), DPAD_LEFT/RIGHT scrubs ±5 s while hidden, pause reveals chrome, INFO/MENU still toggles text mode. | done |
| 2026-08-08 | 5+ | **Second mushaf page source: islamic.app Madinah pages (style “Madinah HD”)**. Researched sources — islamic.app `https://api.islamic.app/v1/mushaf/page/{page}.svg` (CORS-open, 604 standard Madinah pages, per-ayah `data-ayah` tspans, dark theme, 1.45 aspect) ✓ usable; Quran.com image generator (quran/quran.com-images, QCF fonts) ✗ no public CDN; Quran Foundation pages API ✗ requires auth token; alquran.cloud page images ✗ 403. Same standard Madinah pagination as the mp3quran timing `page` field, so page auto-turn syncs unchanged; per-ayah highlight comes from `IslamicPageBands` (DOM-extracted line bands from the page's own data-ayah tspans, unit tested) instead of mp3quran polygons. Style cycle now 0→1→2 (م → ﷽ → HD) via top-bar button or Settings. Verified on emulator: pages 3/4/5 load + auto-turn with the audio, 11/8/5 ayahs indexed per page, chrome hidden fullscreen. 32/32 tests + lint green | done |
| 2026-08-08 | 5+ | **KSU (Ayat) Hafs + Warsh mushaf pages (styles “آيات حفص”/“آيات ورش”)**. Source: quran.ksu.edu.sa (the Ayat KSU reference app) — Hafs pages `ayat/safahat1/{page}.png` (456×672), Warsh pages `warsh/{page}.png` (620×1005), both verified live 200 OK. Hafs pagination verified == standard Madinah == timing `page` field (2:1→2, 2:6→3, 2:255→42, 50:1→518 ✓); Warsh uses its own pagination — the page boundaries are bundled as `KsuWarshPageData` (generated from quran.ksu.edu.sa quran-data.js, Tanzil-sourced facts, 604 pages) with a binary-search `warshPageFor(surah, ayah)`. Raster PNGs have no per-ayah coordinates → highlight = bottom ayah-text strip (semi-transparent) + page-level auto-turn. Style cycle now 0..4 (م ﷽ HD KS WS). Verified on emulator: h5 (456×672) and w5 (620×1005) load, correct page for the playing verse. 35/35 tests + lint green | done |
| 2026-08-08 | 5+ | **Ayah highlighting in ALL mushafs + Hafs Tajweed full-page mushaf + emulator fullscreen**. (1) KSU raster styles now render a real per-ayah highlight: `PageAyahEstimator` (domain, unit-tested) estimates each ayah's band from its text length / the page's total text length — the Madinah mushaf lays text out proportionally, so long ayahs get tall bands, short ones short bands; surah-start pages reserve the header. `PageModeView` gained a fractional-band mode; the old bottom text strip is gone. (2) New style “حفص ملون” (Hafs Tajweed, style 5): KSU full-page tajweed mushaf `tajweed_png/{page}.png` (456×707) with its OWN pagination (Page2 — 25 pages differ from Hafs) bundled as `KsuTajweedPageData`. Style cycle 0..5 (م ﷽ HD KS WS TJ). (3) Emulator fullscreen: `scripts/emulator-fullscreen.py` drives the window via EWMH `_NET_WM_STATE_FULLSCREEN` (python-Xlib; xfwm4 honors the direct-to-window variant) — verified 2560×1440 edge-to-edge; `run-emulator.sh` calls it automatically. Verified on emulator: Warsh page 9 + Tajweed page T9 load with estimated bands (4 ayahs), fullscreen applied. 38/38 tests + lint green | done |
| 2026-08-08 | 5+ | **EXACT KSU line bands from the site's own hilites API**. Discovered `interface.php?ui=pc&do=hilites&mosshaf=<hafs|warsh|tajweed>&t=28&page=<p>` → `{"p": {"s_a": [x, y], ...}}` — the same data the KSU website renders. Reverse-engineered the coordinate space empirically (segmented the tajweed page-9 image into its 17 lines; the API y-values land EXACTLY on lines 3/5/8/15 within ±1 px) → the values are in the page image's NATIVE pixel space (hafs 456×672, warsh 620×1005, tajweed 456×707) — no scaling needed. `KsuHiliteBands.build()` converts y to fraction-of-height bands [y_k, y_{k+1}) (last ayah to page bottom); `KsuHilitesRepository` fetches + caches forever (JsonDiskCache category `ksu_hilites`) with next-page prefetch; `PageAyahEstimator` remains the offline/fallback path. Verified on emulator: tajweed page 188 (9:7–13, 7 ayahs — matches the API exactly) and warsh page 188 both log `EXACT bands`; 40/40 tests + lint green | done |
| 2026-08-08 | 5+ | **Corrected KSU highlight semantics — the site's real hilitePage() algorithm**. Reading `engine.js` closely: the hilites values are where each ayah **ENDS** (not starts), and ayah *k*'s highlight spans from where ayah *k−1* ended to where *k* ends, drawn as up to THREE rectangles: the tail of the previous line (from the left margin to the previous end-x, i.e. this ayah's first partial line), this ayah's last partial line (from its end-x to the right margin), and the full-width block of complete lines between them; same-line ayahs collapse to a single rect. The per-mushaf layout constants (`_hlMeta`) and the special opening-spread layout for pages 1–2 (`prev_top=270`, fp_* overrides) are honored; mid-page surah starts add `fasel_sura`. Implemented as `KsuHiliteGeometry` (domain, 4 unit tests incl. verified tajweed page-9 data and the same-line 1:3/1:4 collapse); `KsuHiliteBands` deleted. `PageModeView` now draws the multi-rect highlight. Verified on emulator: warsh page 192 (9:32–36 = 5 ayahs, matches the API) logs `EXACT rects`; 42/42 tests + lint green | done |
| 2026-08-08 | 5+ | **Real-mushaf two-page spread + page-turn + mushaf picker in the transport bar**. (1) The page view now shows a TWO-PAGE spread like a real mushaf: odd page on the RIGHT, next even page on the LEFT (same layout rule the KSU site uses — `page % 2` → right/left container), forced RTL so it never flips in English locale. `MushafSpreadView` (AnimatedContent keyed on the spread's right-page number): spread changes animate as a page turn (slide-in from the right + fade, reversed when going back), while the highlight moving between the two sides (same spread) is instant. The spread loader consolidates the three page sources — mp3quran SVG, islamic.app, KSU raster — into one `loadSpreadSide()` per side (highlight only on the current-ayah side) with next-spread prefetch. (2) Mushaf selection moved from the top bar into the LOWER transport bar: a compact button showing the current style (م/﷽/HD/KS/WS/TJ) opens a `MushafPickerDialog` (6 styles, D-pad list). (3) Transport buttons compacted (icons 24–34dp, labelMedium, narrower seek bar) so the mushaf keeps more screen. Verified on emulator: tajweed spread (pages 212+213 with exact rects), islamic HD spread (213 right + 214 left + prefetch 215/216), picker dialog opens from the transport and switches styles. 42/42 tests + lint green | done |
| 2026-08-08 | 5+ | **HD (islamic.app) highlight precision — RTL + baseline fixes**. User reported RTL issues and an upward-shifted line band on the HD mushaf. Diagnosis: (a) proved AndroidSVG renders RTL correctly (a بِسْمِ ۝٣٤ probe rendered the ۝٣٤ marker on the LEFT, identical to rsvg — the embedded ayah-end markers are fine); (b) the real bugs were the highlight BANDS: the SVG `<text y>` is the text BASELINE (glyphs sit above it), so the old `(y, y+1.35×font)` band was shifted off the text; and full-width bands bled into the next ayah's text on shared lines. Fix: `IslamicPageBands` now parses structured lines (baseline, font size, anchor, ordered tspans with their ayah keys and text) and `IslamicHiliteRects` computes exact per-ayah rects — same region logic as the KSU site (first partial line from the line's left edge to the previous ayah's end, full middle lines, last partial line from the ayah's end — the embedded ۝ number — to the line's right edge; same-line ayahs collapse to one rect), with tspan text widths measured via Paint (injected `measure` lambda keeps the algorithm pure). Vertical band = baseline − 0.95×font … baseline + 0.35×font (covers the glyphs). Verified on emulator (pixel analysis): the gold highlight now overlaps the text rows exactly on the correct spread side and ends at the ayah number. 43/43 tests + lint green | done |
| 2026-08-08 | 5+ | **HD mushaf text RTL — tspan merging + UI refinements**. (1) The RTL report was real after all, but the bug was AndroidSVG laying MULTIPLE tspans per line in LTR order (each tspan's internal bidi is fine — a single-tspan probe matched rsvg — but a multi-tspan line rendered its first tspan on the LEFT; `direction=rtl` has no effect, verified). The ayah-end markers (۝+digits) are the last tspan content, so they landed on the wrong side. Fix: `IslamicNetworkPageLoader` merges each line's tspans into one for RENDERING (a single drawText lets Android's bidi lay the whole RTL line out correctly — verified by probe: merged 'ألف ألف ألف باء باء' renders ألف on the right) while the ORIGINAL structure is still parsed for the highlight geometry (whose cumulative-width math matches the RTL layout). (2) Seek bar removed from the transport (it grabbed D-pad focus needlessly) — seeking is via prev/next ayah + surah buttons; a plain non-focusable time readout (`30:37 / 45:52`) remains; the hidden-chrome ±5s scrub was removed too. (3) Mushaf chooser simplified: the transport button shows the localized style NAME (المدينة / التجويد / المدينة HD / آيات حفص / آيات ورش / حفص ملون) and the dialog lists each style with a hint + ✓ on the current one, immediate select. (4) The two pages are joined by a folded SPINE (leather-toned vertical ribbon + inner-edge shadows) like a real mushaf. (5) Control-bar auto-hide: delay 4s → 8s, and a new eye toggle in the transport (autoHideControls, persisted in DataStore) disables auto-hide entirely. Verified on emulator: transport shows the time readout only, spine renders between the pages, 8s hide + toggle works (chrome persists with auto-hide off). 43/43 tests + lint green | done |
| 2026-08-08 | 5+ | **Auto-hide timer resets on every button press + chooser hint removed**. (1) The 8s countdown now resets on EVERY key press (a `lastKeyPress` counter bumps in the screen-level key handler and keys the hide LaunchedEffect), so navigating through the transport buttons never hides the chrome mid-interaction; it only hides after 8s of no input. Verified on the emulator with an A/B pixel test (chrome visible right after a key; fullscreen page with the gold highlight after 9s of silence; periodic presses keep it visible). (2) The redundant per-row hint under each mushaf name in `MushafPickerDialog` is removed (the rows show just the name + ✓). 43/43 tests + lint green | done |
| 2026-08-08 | 5+ | **Timing speed correction for compressed reads (عبدالرحمن السويّد / Al-Kahf)**. User reported mushaf↔voice sync off in Surah Al-Kahf for عبدالرحمن السويّد (read 135). Diagnosis: for this read the mp3quran per-ayah timing is systematically ~12% SHORTER than the actual mp3 — surah 18 timing total 1440.5 s vs mp3 1644.4 s (ratio 1.142), surah 2 timing 6039 s vs mp3 6757 s (1.119); verified reads 5/13/17 are 1.000 (timing matches audio exactly). The timing is a uniform compression of a faster rendition, so the highlight drifted progressively AHEAD of the voice (up to ~200 s / 3+ min by the end of a long surah). Fix: `TimingCorrection` (domain, 3 unit tests) computes the linear ratio from the mp3 duration and the timing's last end (clamped 0.80..1.25, ignored under 1.03) and `PlaybackController` maps every playback position into timing space (`pos / ratio`) in the ticker, after seeks, and at resume — recomputed per surah once the mp3 duration is known. Verified on the emulator (resumed the user's own session: السويّد • الكهف): log shows `timing correction 1.142 (mp3 1644s vs timing 1440s)`, and the app's ayah transitions land within 0.1–0.2 s of the corrected timing windows (previously ~5–6 ayahs ahead). 46/46 tests + lint green | done |
| 2026-08-08 | 5+ | **Never estimate ayah sync — unreliable timing shows the surah's first page statically**. User directive: don't estimate ayah start/duration; if the reciter has no exact timings, show the first page of the surah and don't track progress. Replaced the linear speed-correction (`TimingCorrection`) with a reliability check (`TimingAccuracy`): once the mp3 duration is known, it must match the timing's last end within 2%; otherwise the timing is treated as absent — sync is disabled (hasTiming=false, no ayah tracking, no page turn) and the player shows the surah's FIRST page statically (surah.startPage for standard pagination; `warshPageFor(s,1)`/`tajweedPageFor(s,1)` for Warsh/Tajweed) with the “لا يوجد توقيت” notice. Verified on the emulator with read 137 (stretched, ratio 0.788): log `timing unreliable (mp3 5873s vs timing 7458s) — sync disabled`; the spread shows T1+T2 (Al-Baqarah's first page) with no highlight and no page turn over 25 s of playback; text mode shows the verse list without highlight. 47/47 tests (TimingAccuracyTest replaces TimingCorrectionTest) + lint green | done |
| 2026-08-08 | 5+ | **Home auto-scroll to top during playback fixed**. HomeScreen's initial-focus LaunchedEffect was keyed on `ui.lastSession`, which updates every ~5 s while playing (session save loop), so it re-requested focus on the Continue card and yanked the reciter list to the top; keyed on `ui.lastSession?.surahId` (the session's identity) so content updates no longer re-trigger it. Verified on the emulator: with السويّد الكهف playing, scrolled to the و group, waited 8 s — the list stayed put. | done |
| 2026-08-08 | 5+ | **“Only reciters with ayah timing” setting + alphabetical sort in letter groups**. (1) New setting `onlyTimedReciters` (Settings → فقط القراء مع توقيت الآيات, persisted): when ON, Home hides every reciter whose moshafs have no matched timing read (134 of 241 reciters are untimed) and keeps only the timed moshafs per reciter (so the grid's moshaf picker offers only timed ones); the Continue card and recent-reads row follow the same filter. `HomeViewModel` now takes `TimingRepository`, matches moshaf `server` ↔ read `folder_url` (normalized), and re-applies the filter live when the setting changes. (2) Reciters within each letter group are sorted alphabetically (Arabic `Collator`). Verified on the emulator: the م group shows ماجد الزامل، ماهر المعيقلي… with ماجد العنزي (untimed) removed when ON; order alphabetical. 47/47 tests + lint green | done |
| 2026-08-10 | 5+ | **Mushaf selection first for multi-moshaf reciters**. Bug: opening a reciter from Home (chip or search) always took `moshafs.firstOrNull()` — reciters with several moshafs silently opened the FIRST one; the picker only existed inside the surah grid ("تغيير المصحف"). Fix: reciters with >1 moshaf now show a `MoshafSelectionDialog` BEFORE the grid (✓ marks the current one; initial focus on it; back dismisses without navigating). The dialog was extracted from the SurahGrid's private copy into `ui/components/MoshafSelectionDialog.kt` (shared by Home chips, search results, and the grid's change-moshaf button). `HomeViewModel.requestMoshafSelection()` decides via moshaf count and holds the pending reciter id in state so the dialog survives recomposition. Verified on emulator: أحمد الحذيفي (1 مصحف) → grid directly; أحمد الطرابلسي (2 مصحف) → "اختر المصحف" dialog first, selecting قالون opens the grid with قالون, back dismisses to Home; تغيير المصحف still opens the shared dialog with "✓ حفص عن عاصم - مرتل". 47/47 tests + lint green | done |
| 2026-08-10 | 5+ | **KSU highlight geometry fixed (native-space) + Hafs Tajweed is now the default mushaf**. User report: the ayah highlight "stops about mid height of line in the lower half of the page & gets progressively worse" on the KSU mushafs. Diagnosis (empirical, pixel-level): the hilites API (`interface.php?do=hilites`) returns ayah-end x/y in the page image's NATIVE pixel space — verified on the real PNGs for all three mushafs (tajweed p9 2:61 y=648 = vertical center of the last line's ink 642..666; hafs p3 2:16 y=616 ↔ line center 612 of 456×672; warsh p192 9:36 y=685 ↔ 683 of 620×1005). But `KsuHiliteGeometry.build` converted to page fractions by dividing by `Meta.displayHeight` (720/690/760) instead of the image height (707/672/1005) — a drift proportional to y that grows down the page: ~12px short at the tajweed bottom line (highlight stops ~40% up the ink), ~16px hafs, and ~220px for warsh. Fix: fractions are now computed directly against the image's native dimensions (spaceW = imageWidth, spaceH = imageHeight); `displayHeight` removed from `Meta`. The line band [y−ofheight, y−ofheight+height] is centered on the API y = the line's ink center, so it now covers the full line ink. (2) `mushafStyle` default is now 5 = Hafs Tajweed (حفص ملون) for fresh installs (was 0 = Madinah SVG). Added a regression test pinning the last-line band for the verified 2:61 data in native fractions. Verified: EXACT-rects path logs on the emulator (توفيق الصايغ, tajweed default, `ksu EXACT rects page 2 (tajweed): 5 ayahs` + T1–T4 pages load); 48/48 tests + lint green | done |
| 2026-08-10 | 5+ | **"Only reciters with ayah timing" now filters the surah grid too**. Previously the setting filtered reciters/moshafs on Home only; the surah grid still listed every surah of the moshaf (incl. ones with no per-ayah timing files). Now, when the setting is ON, `SurahGridViewModel` intersects the moshaf's surah list with the matched read's timed surahs (`TimingRepository.surahsWithTiming(readId)` — the `ayat_timing/soar` list, cached forever like the rest of the timing data; null on failure → keeps the full list, graceful). The VM observes `onlyTimedReciters` from DataStore and re-filters live when the setting changes (same pattern as HomeViewModel). Verified on emulator: نبيل الرفاعي (read 87, timing covers only 113 of 114 surahs — surah 27 النمل missing) — setting ON → grid shows 113 surahs, 27 absent; setting OFF → 114 surahs, 27 back. 48/48 tests + lint green | done |
| 2026-08-10 | release | **v1.1 released to the samba share** (`/mnt/rpi-share`). Bumped `versionCode` 1→2 / `versionName` "1.0"→"1.1" (the app now carries the three 2026-08-10 fixes: multi-moshaf selection dialog first, KSU highlight native-space fix, "only timed" surah-grid filter). Pushed as `QuranTV-1.1-debug.apk` (24.3 MB, MD5 507ae54da3ea8ab3e028ca49b652facf, verified copy); `QuranTV-1.0-debug.apk` kept for rollback. aapt-verified: versionCode 2 / versionName 1.1 | done |
| 2026-08-10 | 5+ | **Warsh highlight space corrected (display-space per mushaf) + v1.2**. User report after the native-space fix: "this messed up the warsh highlighting". Investigation: the hilites API's coordinate space is NOT uniform — Hafs/Tajweed are served at NATIVE size (456×672 / 456×707; gap-pitch analysis 55/60 and 66/68 near-integer for native vs 47/60 and 57/68 for the old 690/720 display heights), but Warsh is DISPLAY-scaled: all 49/49 ayah gaps on warsh pages 190–199 fit the 46.2px display pitch (native 61px only fits 4/49) → the site renders the 620×1005 warsh page at height 760 and the API reports in that space. Fix: `Meta.displayHeight` restored as the per-mushaf API-space height — HAFS 672, TAJWEED 707 (native), WARSH 760 (display) — with `spaceW = imageWidth × displayHeight / imageHeight`. New regression test pins warsh p192 9:36 (665/760 top). Emulator-verified precisely: with warsh pages playing (توفيق الصايغ, s2), the 2:52→2:53 highlight movement measured on screen spans y=370..491 vs expected display-space band 369.5..491.7 (±1px). Released as **v1.2** (versionCode 3) to the samba share (`QuranTV-1.2-debug.apk`, MD5 a95ec3259f05494e81b056f692306bd0); 49/49 tests + lint green | done |
| 2026-08-10 | 5+ | **TV screensaver no longer starts during playback + v1.3**. User report: "tv goes into screensaver mode even when running app". Root cause: the app never set `FLAG_KEEP_SCREEN_ON` — after the system's screensaver timeout (default several minutes) with no D-pad input (the chrome auto-hides during playback), the TV's daydream (Backdrop) dims the display while the audio keeps playing. Fix: `PlayerScreen` sets `view.keepScreenOn = ui.isPlaying` in a `DisposableEffect` — the flag is applied while playback is active, cleared on pause and when leaving the player (no permission needed; daydreams only start when the display would sleep, which keepScreenOn prevents). Verified on the TV emulator: window attrs show `KEEP_SCREEN_ON` while PLAYING and it disappears on PAUSE; with `screensaver_timeout` shortened to 15 s the screen stays Awake with app focus after 25 s of no input during playback (the emulator image lacks the dream service, so the Backdrop itself can't be triggered there — the flag mechanism is the documented approach used by media apps). 49/49 tests + lint green. Released as **v1.3** (versionCode 4) to the samba share (`QuranTV-1.3-debug.apk`, MD5 c679e10470ee8b92642f31bcb9a7f2d9) | done |
| 2026-08-10 | 5+ | **Warsh drift re-investigation — current code pixel-verified exact; v1.4 adds a version indicator**. User report after v1.2/v1.3: "warsh still drifting highlight to about 60% up in lower pages". Re-verified the display-space (760) mapping exhaustively: line-pitch fit S≈1.330 (≈1005/760) on pages 2–590 with 3–6px residuals, API gap-multiples self-consistent on the last pages (596–604) too; pagination (KsuWarshPageData ↔ API first-ayah) matches 0/25 sampled pages; on-screen diff tests on the TV emulator are EXACT at three mushaf positions: pages 7–8 (2:52→2:53 moved y=370..491 vs expected 369.5..491.7), page 518 surah 50 (13→14 union observed 0.754..0.928 vs expected 0.754..0.929), and the LAST page 604 (113:5 observed 0.518..0.568 vs expected 0.518..0.571). The reported "~60% up" fingerprint matches v1.1's native-space bug exactly (e.g. surah 50 p518 ayah 14 band rendered at 0.62–0.66 in v1.1 vs 0.82–0.93 now) — strongly suggesting a stale APK on the device. To make the installed version verifiable, the About line in Settings now appends the version name (e.g. "· v1.4", read from PackageManager). Released as **v1.4** (versionCode 5) to the samba share (`QuranTV-1.4-debug.apk`, MD5 70e26533d3dfcabb27b6c1350e335f63); 49/49 tests + lint green | done |
| 2026-08-10 | 5+ | **Home search fixed: search button + Enter now work + v1.5**. User report: "no search button & nothing on pressing enter". Investigation on the TV emulator: the search field gained focus but `mServedInputConnection=null` — the system IME never connects for the focused text field on TV (no keyboard appears, `input text` goes nowhere). Fix in `SearchOverlay`: (1) robust focus acquisition (retry loop, matching the other screens) followed by an explicit `LocalSoftwareKeyboardController.show()` so the keyboard reliably opens; (2) `KeyboardOptions(imeAction = ImeAction.Search)` + `KeyboardActions(onSearch = …)` — the keyboard now shows a SEARCH key and pressing it opens the first matching reciter; (3) a root-level `onPreviewKeyEvent` maps the remote's Enter/DPAD_CENTER (while the field is focused) to the same first-match open, covering remotes/IMEs that never deliver the IME action; (4) a visible on-screen **بحث (Search) button** + a one-line hint ("اضغط بحث أو Enter لفتح أول نتيجة") — a guaranteed D-pad target that works even on TVs where no keyboard ever appears. The open-result logic is shared between the results list, the IME action and the button (multi-moshaf reciters still open the chooser first). Verified on the emulator: overlay shows the hint + بحث button, D-pad moves field → button → results, OK is a no-op with an empty query, no crashes. Typing itself can't be exercised on this emulator image (it ships GBoard, which refuses to serve a TV-profile input connection — real TVs use the Leanback keyboard, which the explicit show() + IME action now drive). 49/49 tests + lint green. Released as **v1.5** (versionCode 6) to the samba share (`QuranTV-1.5-debug.apk`, MD5 2b392bb9d624572adb13c50723514fb4) | done |
| 2026-08-10 | 5+ | **Amiri Quran font + seamless surah transitions + v1.6**. (1) **Quran font**: bundled Amiri Quran (SIL OFL, 137 KB — a naskh face designed for the uthmani mushaf script with full diacritics) at `res/font/amiri_quran.ttf`; exposed as `QuranFontFamily` in the theme and applied ONLY to Quran content — the ayah text in `TextModeList` and the basmala header in the player. UI chrome keeps the system font. (2) **Seamless surah transitions**: `PlaybackController.playSurah` now queues the whole remaining surah sequence as one playlist (mediaId = surah id) when repeat is OFF — ExoPlayer pre-buffers and transitions between surahs with no gap; `onMediaItemTransition` fires `onSurahAdvanced` → `PlayerViewModel.handleSurahAdvanced` swaps timing/text state via `attachSurah` (no audio re-prepare). Surah repeat uses `REPEAT_MODE_ONE` (loops the current item; ALL would loop the whole queue). Next/prev surah now use `seekToNext/PreviousMediaItem` (instant, remote media-key friendly). State is updated BEFORE `setMediaItems` so the transition listener doesn't fire spuriously on play. Verified on the emulator: surah 113→114 transitions with `media transition -> surah 114 (reason 1)`, playback stays PLAYING (no STATE_ENDED, no re-buffer), position continues, text mode auto-shows الناس with the new basmala/verses; 114 plays to its end then stops (end of playlist). 49/49 tests + lint green. Released as **v1.6** (versionCode 7) to the samba share (`QuranTV-1.6-debug.apk`, MD5 1b7110b866717dce151130b3d6cea2b6) | done |
| 2026-08-10 | 5+ | **Removed the redundant display-mode toggle from the player top bar + v1.7**. The top bar's book/TextFields icon (which switched text ↔ mushaf page mode) duplicated the mode toggle already present in the lower transport bar (and the INFO/MENU key). The player top bar now holds only the back button, surah name, reciter • moshaf and the no-timing notice. Verified on the emulator: the top 100px contain exactly one clickable (the back button). 49/49 tests + lint green. Released as **v1.7** (versionCode 8) to the samba share (`QuranTV-1.7-debug.apk`, MD5 94ce9ab0d42ab2f0ca8f0821191ae43d) | done |
| 2026-08-11 | 5+ | **Transport bar redesigned into logical zones + v1.8**. The bar is now organised for easy D-pad scanning: (1) **RIGHT zone** (RTL start): display-mode toggle next to the auto-hide eye — "display mode close on right" as requested; (2) **CENTER zone**: time readout + the playback cluster (prev surah · prev ayah · play · next ayah · next surah) dead centre — the surah buttons' visual order is swapped so the bar reads prev→next left-to-right (previously next-surah was leftmost in RTL); the cluster is emitted direction-aware (reversed child order in RTL) so the on-screen order is the same in Arabic and English, with non-auto-mirrored skip/chevron icons that keep the standard outward-pointing media look; (3) **LEFT zone** (RTL end): repeat · speed · mushaf style · jump-to-surah. Repeat now uses a representative Repeat/RepeatOne icon (tinted gold when active) instead of a text label. Verified on the emulator (Arabic/RTL): button x-positions read left→right [السور 92][حفص ملون 222][1× 342][repeat 435] | [prev-surah 656][prev-ayah 721][play 795][next-ayah 909][next-surah 1013][time 1174] | [auto-hide 1730][mode 1830]; jump dialog opens; no crashes. 49/49 tests + lint green. Released as **v1.8** (versionCode 9) to the samba share (`QuranTV-1.8-debug.apk`, MD5 5d46bcc7d6342db5ca61e79b49d19543) | done |
| 2026-08-11 | 5+ | **Transport cluster: next left of play / prev right + no-timing mushaf page browse + v1.9**. (1) The playback cluster's on-screen order is now **next surah · next ayah · play · previous ayah · previous surah** (next buttons left of play, previous right — the RTL reading order); arrow directions unchanged (verified pixel-exact: the directional icons auto-mirror in RTL, so they are un-mirrored via `Modifier.scale(-1)` when RTL to keep the standard outward look). (2) When a surah has **no ayah timing** (لا يوجد توقيت), the prev/next ayah buttons now browse the mushaf page by page instead of jumping surahs: `noTimingPage` state drives the static spread (each press turns a full two-page spread, clamped to 1..604) and resets on surah change / when timing appears. Verified on the emulator with أحمد الحذيفي (untimed): next-ayah advances T1/T2 → T3/T4, prev-ayah returns and clamps at page 1; the cluster reads left→right next-su(656) · next-ay(711) · play(808) · prev-ay(922) · prev-su(1026). 49/49 tests + lint green. Released as **v1.9** (versionCode 10) to the samba share (`QuranTV-1.9-debug.apk`, MD5 5faa11f8d0776520e2f35ec422e56bf3) | done |
| 2026-08-11 | 5+ | **Time readout moved out of the center + next-surah button always visible + v1.10**. User report: "can't see next surah button" — the time readout (elapsed / total) shared the center zone with the playback cluster, squeezing the leftmost cluster button (next surah) on narrower screens. The time now sits at the FAR LEFT zone (the other side of the playback control — RTL end), and the center zone holds only the cluster [next surah · next ayah · play · previous ayah · previous surah]. Verified on the emulator: left→right = time(127) · السور(271) · حفص ملون(401) · 1×(521) · repeat(614) | next-su(742) · next-ay(846) · play(960) · prev-ay(1074) · prev-su(1178) | eye(1730) · mode(1830) — every button clearly spaced, next surah fully visible. 49/49 tests + lint green. Released as **v1.10** (versionCode 11) to the samba share (`QuranTV-1.10-debug.apk`, MD5 19613fd3b642268286efa3af660213b3) | done |
| 2026-08-11 | 5+ | **Transport: mushaf style next to the mode toggle + time left of next-surah + v1.11**. (1) The mushaf style button (e.g. حفص ملون) moved from the left zone into the right zone, immediately LEFT of the display-mode toggle: right zone now reads eye · style · mode. (2) The time readout moved from the far-left zone into the center, immediately LEFT of the next-surah button (time · next-su · next-ay · play · prev-ay · prev-su); the left zone now holds just repeat · speed · jump. Verified on the emulator: time at x=690, next-su at 784, حفص ملون at 1702, mode at 1829; the mushaf picker opens from its new spot. Also answered the user's الحذيفي question: أحمد الحذيفي (id 205) has NO timing because mp3quran's `ayat_timing/reads` dataset doesn't include his audio at all (not an API error/limit); علي بن عبدالرحمن الحذيفي (id 74) DOES have timing for all 114 surahs, but mp3quran's timing for him is INCOMPLETE for many surahs (e.g. surah 1's timing stops at ayah 5 of 7 — last end 45.7s vs the actual 63.0s mp3, ratio 0.73), so the app's accuracy gate (within 2%) correctly disables sync — surah 2 matches (0.99) and syncs fine. 49/49 tests + lint green. Released as **v1.11** (versionCode 12) to the samba share (`QuranTV-1.11-debug.apk`, MD5 573f8f85247dc2324f59aa4e4e8f5ac2) | done |
| 2026-08-11 | 5+ | **Transport fixes: horizontal time, combined display-mode + mushaf chooser, wider center + v1.12**. User report: the time readout rendered VERTICALLY (each character stacked — it was squeezed to a ~15px column). Root cause: at the TV's density-2 scale each 48dp button is 96px, so the 5-button cluster (~544px) + the time's 130dp min width (~260px) overflowed the old 1/3-wide center zone (~624px), collapsing the time to one character. Fixes: (1) the center zone is now wider (zone weights 0.8/1.4/0.8) so cluster + time fit; (2) the time readout is emitted OUTSIDE the slot list (direction-aware) with `maxLines=1` + `widthIn(min=130dp)` — renders on ONE horizontal line immediately LEFT of the next-surah button (verified: 154px-wide single-line box at x=638..792, next-su at 870). (3) The separate text/image mode-toggle icon button is REMOVED from the transport — `MushafPickerDialog` is now a combined list: display mode (نص / صفحة المصحف) + mushaf style (6 styles), opened from the single mushaf button (the style name label); `PlayerViewModel.setDisplayMode(mode)` added. Verified on the emulator: combined dialog lists both sections with ✓ marks, selecting نص switches to text mode; the bar is decluttered (left: السور/1×/repeat · center: time+cluster · right: eye/حفص ملون). 49/49 tests + lint green. Released as **v1.12** (versionCode 13) to the samba share (`QuranTV-1.12-debug.apk`, MD5 a23994a992529600ccd8970ff45e8485) | done |
| 2026-08-11 | 5+ | **Mushaf chooser simplified to one flat list + selecting a style enters page mode + v1.13**. `MushafPickerDialog` is now a single list (no sections), top → bottom exactly as requested: حفص ملون (Ayah hafs colored) · آيات حفص (Ayat hafs) · آيات ورش (Ayat warsh) · المدينة (Almadinah) · المدينة HD (Almadinah HD) · التجويد الملون (Tajweed colored) · نص (Text). ✓ marks the current selection and initial focus lands on it. Also fixed: picking a mushaf style from the list now ALSO sets displayMode=1 (page) — previously it only changed the style, leaving the user in text mode. Verified on the emulator: the flat list renders in the requested order; selecting آيات حفص sets style 3 + page mode and loads the H (456×672) pages; selecting نص returns to text mode. 49/49 tests + lint green. Released as **v1.13** (versionCode 14) to the samba share (`QuranTV-1.13-debug.apk`, MD5 046ace3f365febc43cba8ae21fa3c22d) | done |
| 2026-08-11 | 5+ | **Overlay chrome in page mode + 3s auto-hide + single-mushaf cards simplified + v1.14**. (1) In page mode the mushaf now ALWAYS fills the screen; the top bar and transport are translucent OVERLAYS (0.62-alpha scrim — the page shows through) that appear on a key press and auto-hide — no more full-block layout squeezing the page. Text mode keeps the normal bar layout (bars always visible around the verse list). (2) Auto-hide timeout reduced 8 s → 3 s. (3) Home reciter cards no longer show "1 مصحف" for single-mushaf reciters (the count only appears when >1). Verified on the emulator: with chrome visible the page stays full-screen under a dark translucent scrim; 4.5 s after the last key the bars vanish (screen diff: top/bottom change ~320); the reciter row shows no count for 1-moshaf reciters and "2 مصحف" for multi-moshaf ones. 49/49 tests + lint green. Released as **v1.14** (versionCode 15) to the samba share (`QuranTV-1.14-debug.apk`, MD5 3165bd6949301c850629f0ced20c77d1) | done |
| 2026-08-11 | 5+ | **Home reciters in wrapped rows + transport time relocated + v1.15**. (1) Each letter group on Home now wraps its reciter chips into MULTIPLE short rows (FlowRow, ~5 chips per row on the TV) instead of one long horizontally-scrolling LazyRow — every reciter is a few D-pad presses away. (2) The elapsed/total time readout moved from the center zone to the RIGHT end of the LEFT section (السور · 1× · repeat · time), so the center zone holds ONLY the playback cluster — the media buttons are now dead-centre. Zone weights back to equal thirds. Verified on the emulator: the أ group renders rows of 5 (أحمد الحذيفي..أحمد النفيس / أحمد بن علي العجمي..أحمد صابر / أحمد طالب بن حميد..أخيل عبدالحي روا); the transport reads left→right السور(93) · 1×(184) · repeat(278) · time(512) | next-su(741) · next-ay(845) · play(959 ≈ screen centre 960) · prev-ay(1073) · prev-su(1177) | style(1700) · eye(1829). 49/49 tests + lint green. Released as **v1.15** (versionCode 16) to the samba share (`QuranTV-1.15-debug.apk`, MD5 da8ccbc2011bb936a907739f701aa190) | done |
| 2026-08-11 | 5+ | **Change the reciter from the transport bar + v1.16**. Added a mic (🎤) button in the transport's right zone (eye · reciter · mushaf button) that opens a new `ReciterPickerDialog` — a flat D-pad list of all reciters with ✓ on the current one and initial focus on it. Picking a multi-moshaf reciter chains into the existing `MoshafSelectionDialog`; single-moshaf reciters switch directly. `PlayerViewModel.switchReciter(reciter, moshaf)` keeps the CURRENT surah (falling back to the moshaf's first surah when it doesn't cover it) and restarts it from the first ayah — no need to back out to Home. `PlayerViewModel` now takes `CatalogRepository` to resolve the new moshaf's surah list. Verified on the emulator: from the player, the 🎤 button opens the picker; selecting ماهر المعيقلي → his 3-moshaf chooser → picking حفص عن عاصم restarts الكهف with the new reciter (top bar: "ماهر المعيقلي • حفص عن عاصم - مرتل", EXACT rects flowing). 49/49 tests + lint green. Released as **v1.16** (versionCode 17) to the samba share (`QuranTV-1.16-debug.apk`, MD5 b2e57673b0d810824df4a1ef3474f35d) | done |
| 2026-08-11 | 5+ | **Reciter switch resumes from the SAME ayah + v1.17**. `switchReciter` now captures the current timing index before the switch and passes it as `startAyahIndex` to `play()`; the new reciter's timing entry for that ayah provides the start position (best effort — ayah boundaries align across reciters even though absolute times differ; falls back to the ayah's start or 0). The surah is kept (fallback to the moshaf's first when it doesn't cover it); the ayah is only carried when the SAME surah is kept. Also fixed: `ReciterPickerDialog` now scrolls to and focuses the CURRENT reciter (the focus row was previously off-screen for long lists, so the dialog opened without focus). Verified on the emulator: الحصري on الكهف at ayah 7 (~76 s) → switched to مشاري العفاسي → the new playback started at العفاسي's ayah 7 (~76 s), position continued 76→82 s. 49/49 tests + lint green. Released as **v1.17** (versionCode 18) to the samba share (`QuranTV-1.17-debug.apk`, MD5 080016735d9f97e3d8d395baab39c047) | done |
| 2026-08-11 | 5+ | **Reciter picker: alphabetical sort + live search filter + v1.18**. `ReciterPickerDialog` now sorts the list with the Arabic collator (مشاري → مصطفى → معمر → معيض → مفتاح → منصور — previously the raw API order) and has a search field at the top that filters as you type (name contains / letter match). The keyboard's search key and the remote Enter/OK (while the field is focused) open the first match; the dialog opens with the search field focused (keyboard shown explicitly) and the list pre-scrolled to the current ✓ reciter. Verified on the emulator: sorted order + the search field present (typing itself can't be exercised — the emulator has no TV keyboard, same as the Home search). 49/49 tests + lint green. Released as **v1.18** (versionCode 19) to the samba share (`QuranTV-1.18-debug.apk`, MD5 525dd44f7895a7fabb6398561bee3e05) | done |
| 2026-08-11 | 5+ | **Home reciter search: shows the full list + Arabic-tolerant filter + sorted + v1.19**. User report: "reciter name search in home screen not filtering". The filter code was correct, but the overlay started EMPTY (empty query → empty list), so there was nothing to see until typing worked — and typing can't be exercised on this emulator (its GBoard refuses to serve a TV-profile input connection, mServedInputConnection=null). Fixes: (1) an EMPTY query now returns the whole reciter list — the overlay is a browsable list and typing visibly narrows it; (2) the filter is extracted as a pure `reciterMatchesQuery` with Arabic-tolerant normalization (hamza variants أ/إ/آ/ٱ → ا, ة → ه, ى → ي) so "احمد" matches "أحمد", "مصطفي" matches "مصطفى" etc.; (3) results are sorted with the Arabic collator. Added 7 unit tests using real reciter names (exact, partial, hamza/maqsura/marbuta normalization, letter match, empty query, non-match) — all pass. Verified on the emulator: the overlay lists reciters on open (إبراهيم الأخضر، إبراهيم الجبرين، إبراهيم الدوسري…) in alphabetical order. 56/56 tests + lint green. Released as **v1.19** (versionCode 20) to the samba share (`QuranTV-1.19-debug.apk`, MD5 95301e13178b34b17edb05854b5ab800) | done |
| 2026-08-11 | 5+ | **"No timing" notice moved to the transport bar + v1.20**. The "لا يوجد توقيت" text was removed from the player's top bar and now appears in the transport bar's right zone, immediately LEFT of the show/hide (auto-hide eye) button. Verified on the emulator (a no-timing reciter): transport reads left→right السور(92) · 1×(186) · repeat(279) · time(471) | cluster(742..1178) | لا يوجد توقيت(1436) · eye(1574) · reciter(1674) · style(1802). 56/56 tests + lint green. Released as **v1.20** (versionCode 21) to the samba share (`QuranTV-1.20-debug.apk`, MD5 8160be70f40d1f84fdeab8cc10ee7b07) | done |
| 2026-08-11 | 5+ | **No-timing visual indication in the reciter / mushaf / surah choosers + v1.21**. Reciters, moshafs (riwayat) and surahs that have NO ayah timing are now dimmed (onSurfaceVariant) with a small "بدون توقيت" badge in the three choosers: `ReciterPickerDialog` (a reciter is untimed when none of its moshaf servers matches a timing read), `MoshafSelectionDialog` (per-moshaf server match) and `SurahJumpDialog` (surahs outside the matched read's `ayat_timing/soar` list — e.g. read 87 lacks surah 27). Added `TimingRepository.timedServerUrls()` (normalized read folders) and `NoTimingBadge()` in Common.kt; `HomeViewModel` exposes timedServerUrls in state; `SurahGridViewModel` computes timedServerUrls + untimedSurahIds in loadSurahs; PlayerScreen computes both for its dialogs. Verified on the emulator: the reciter picker shows "بدون توقيت" next to أخيل عبدالحي روا، أستاذ زامري، إسلام صبحي، رشيد إفراد، جمال شاكر عبدالله… while timed reciters have none; the player jump dialog opens cleanly with the new param (the emulator's rail/D-pad was too flaky to reach the specific read-87 surah-jump case — the surah/moshaf badges share the exact same verified pattern). 56/56 tests + lint green. Released as **v1.21** (versionCode 22) to the samba share (`QuranTV-1.21-debug.apk`, MD5 68c536d222bd0405e595e5a686985dcd) | done |
| 2026-08-11 | 5+ | **No-timing badges on the Home reciter cards + the surah grid + v1.22**. The "بدون توقيت" badge + dimming now also appears on the MAIN Home reciter chips (untimed = no moshaf server matches a read — e.g. أحمد الحذيفي) and on the surah selection grid cards (untimed = outside the matched read's soar list). Fixed a logic gap found during testing: when a reciter has NO read at all, every surah is untimed (previously the untimed set stayed empty because it was only computed when a read matched) — fixed in both SurahGridViewModel and PlayerScreen. Verified on the emulator: Home chip أحمد الحذيفي shows the badge; his surah grid shows "بدون توقيت" under every surah card. 56/56 tests + lint green. Released as **v1.22** (versionCode 23) to the samba share (`QuranTV-1.22-debug.apk`, MD5 98cc8aa55447029b4743d3e3b7b2ff37) | done |
| 2026-08-11 | 5+ | **Badge accuracy: probe the real mp3 duration per surah + v1.23**. User report: سورة القدر by محمود علي البنا (المجود) showed the timed badge but actually has no usable timing. Root cause: the badge trusted the mp3quran `ayat_timing/soar` list, which OVER-CLAIMS — verified: read 122 (البنا المجود) lists 114 surahs, but the real mp3s don't match the timing for several (s97 84.1 s mp3 vs 79.1 s timing, ratio 0.94 → outside the 2% accuracy gate → sync correctly disabled at playback, so the app shows "لا يوجد توقيت"). The soar list can't tell; only comparing the mp3 duration against the timing total can. Fix: `TimingRepository.timingUsability(readId, surahId, mp3Url)` probes each surah's mp3 duration via MediaMetadataRetriever (skipped for files >10 MB so huge surahs don't get eagerly downloaded) and checks `TimingAccuracy.isReliable`; the verdict is cached forever (`usable_r<read>_s<surah>`). The surah grid calls it per visible card (`refineSurahTiming`) and marks the card untimed when the verdict is false — so the badge now reflects REAL usability, not the soar claim. Verified on the emulator with عبدالمحسن الحارثي (read 66): cache gained usable_r66_s1..s48 from the visible cards; s1 correctly marked unusable (mp3 39.2 s vs timing 37.1 s, 5.7 % off) and its grid card shows "بدون توقيت", while s31 (0.4 % off) stays timed — the exact case the user reported behaves identically. 56/56 tests + lint green. Released as **v1.23** (versionCode 24) to the samba share (`QuranTV-1.23-debug.apk`, MD5 c6f9692a0fc1728719d01fded548083f) | done |
| 2026-08-11 | 5+ | **Timing accuracy gate fixed: trailing-silence-aware + v1.24**. User pushback: "aren't the timings accurate as they are from api & your tooling for getting duration is flawed". They were RIGHT. Investigation: mp3quran timings ARE accurate for البنا المجود s97 (القدر) — the file is 84.1 s but the recitation ends at 80.9 s (3.2 s TRAILING SILENCE); the timing's 79.1 s total matches the recitation. The old 2%-symmetric gate compared the FULL file duration (incl. silence) against the timing total → 84.1 vs 79.1 (ratio 0.94) → wrongly disabled sync, and the v1.23 badge (same gate) wrongly marked it untimed. Also found the reverse error: read 17 s114's timing OVER-CLAIMS (39.1 s timing vs a 31.0 s file, −20.7%) — genuinely wrong. Fix: `TimingAccuracy.isReliable` is now ASYMMETRIC — the mp3 may be LONGER than the timing by up to max(8 s silence allowance, 2% relative) (trailing silence on short and long surahs alike), while the timing may only over-claim the mp3 by ≤2% (rejects over-claims and stretched reads). Calibrated against 17 real files: accepts the accurate+silent cases (البنا s97 +5.0 s, s1 +6.2 s, all good reads 0.2–3.8 s, 2-hour surah +17 s = 0.24%) and still rejects the genuine errors (البنا s114 +34 s truncated, الحذيفي s1 +17 s truncated, read 135 s1 +9.4 s / s2 +718 s compressed, read 17 s114 −20.7% over-claim, read 137 s2 stretched). The badge probe uses the same gate (verdict cache re-keyed usable2_ so stale verdicts are ignored). 4 new unit tests. Verified on the emulator: البنا المجود سورة القدر now SYNCES — "ayah 2 @ 16751ms" + "EXACT rects page 598", no "لا يوجد توقيت" notice. 60/60 tests + lint green. Released as **v1.24** (versionCode 25) to the samba share (`QuranTV-1.24-debug.apk`, MD5 be7513270bc088a48ccf6273e6ddc841) | done |
| 2026-08-11 | 5+ | **Tafseer panel on the non-reciting mushaf page + v1.25**. New option "التفسير في صفحة المصحف الأخرى" (Settings): when ON, the mushaf spread page that is NOT reciting shows a tafseer panel for the current ayah instead of the page image. Data source: the KSU Ayat app's bundled SQLite databases — `tarajem/ar_muyassar.ayt` (التفسير الميسر — simplified tafseer), `ar_ma3any.ayt` (معاني الكلمات — word meanings), `en_sahih.ayt` (English Sahih International translation) — extracted from the official Ayat Linux package, each a `(sura, aya, text)` table with one row per ayah (6236). Bundled as app assets (~6.8 MB, compressed ~2.3 MB in the APK → 26.7 MB total) and copied to files on first use (`TafseerRepository`, in-memory cached). The panel shows: verse reference (114 : 6), the ayah text in the Amiri font, then المعاني (word meanings), التفسير الميسر, and الترجمة — scrollable with the D-pad (focus is given to the panel in fullscreen so UP/DOWN scroll it; any other key reveals the chrome), fresh scroll per verse, `<br>` in meanings converted to newlines. `MushafSpreadView` gained the optional tafseer params; PlayerScreen tracks the current side (highlight side) so the panel lands on the OTHER page. Verified on the emulator (البنا المجود, surah 114): the left spread side is the dark panel with "114 : 6", the ayah text, معاني الكلمات ("الجـِنّة : الجـِـنّ"), التفسير الميسر ("من شياطين الجن والإنس.") and الترجمة, while the right side is the mushaf page (bright); no crashes on auto-hide/scroll. 60/60 tests + lint green. Released as **v1.25** (versionCode 26) to the samba share (`QuranTV-1.25-debug.apk`, MD5 6be194d4f728fd61cee718bf43593fbd) | done |
| 2026-08-11 | 5+ | **Page view selector: Surah image / simplified tafseer / word meanings / translation + v1.26**. Redesigned per user feedback: instead of a tafseer panel beside the reciting page, the WHOLE page area now switches between four views via a button in the top bar (label shows the current view: مصحف/تفسير/معاني/ترجمة) opening a 4-option chooser (✓ on current). Views: (1) Surah image — the normal two-page mushaf spread; (2) التفسير الميسر — the WHOLE surah's simplified tafseer, one ayah per row; (3) معاني الكلمات — the whole surah's word meanings; (4) الترجمة — the whole surah's English translation. The content views use a text-mode-style list (`SurahContentView`): the current ayah is highlighted and AUTO-SCROLLS with the recitation when timing exists (same pin-to-top + proportional-overflow follow as text mode; static without timing). Data: `TafseerRepository.surahContent(surahId, mode)` loads the whole surah per mode (SQLite query, cached). The old other-page panel + showTafseer setting were removed (MushafSpreadView reverted to its clean form). Verified on the emulator: the top-bar button (label updates مصحف→تفسير→معاني→ترجمة) opens the chooser; tafseer view shows سورة البقرة ayahs 8–12 with the current ayah 9 in view (auto-scroll), meanings view shows "خلوا إلى شياطينهم : انصرفوا إليهم..." per ayah, translation view shows "Alif, Lam, Meem." / "This is the Book about which there is no doubt..." per ayah; switching back to مصحف restores the bright spread. 60/60 tests + lint green. Released as **v1.26** (versionCode 27) to the samba share (`QuranTV-1.26-debug.apk`, MD5 833baf24ff7b131e9e6e4132748c4b00) | done |
| 2026-08-11 | 5+ | **Mushaf page always visible — context views become an optional side panel + auto-hide 5 s + empty meanings hidden**. Redesigned the player's page-area view selector per user request: the mushaf spread (with the highlighted ayah) is now ALWAYS on screen; picking التفسير الميسر / معاني الكلمات / الترجمة from the top-bar dropdown shows that content as a SIDE PANEL beside the mushaf (60/40 split, forced RTL so the mushaf stays on the right in both UI languages) — the viewer sees the highlighted ayah and its context at the same time. The dropdown's first option is now "صفحة المصحف فقط" (mushaf full screen, no panel) with a hint under the dialog title explaining the new behavior; the top-bar button label shows the current side view (مصحف/تفسير/معاني/ترجمة). The panel reuses `SurahContentView` (new `contentPadding`/`fontSizeSp`/`rowSpacing` params for the narrower width): current ayah highlighted + auto-scroll; rows with EMPTY content are hidden (2,576 of the 6,236 word-meaning rows in the KSU DB are empty — e.g. surah 2 ayahs 1, 3, 4, 6, 8…), and when the current ayah itself has no row the list pins to the nearest preceding row (verified: ayah 149 empty → panel pinned at 147). Also: page-mode chrome auto-hide 3 s → 5 s from the last key press. Verified on the emulator (أحمد عيسى المعصراوي • البقرة, Hafs Tajweed): tafseer panel pinned at the playing ayah (111 → 141 → 149), meanings shows rows 2,5,7,9,10,14… (empties gone), translation at 149, mushaf-only restores the full-screen spread, the mushaf highlight band sits on the correct spread side (ayah 114 on the left page, y 885–933), the bar is visible 3.5 s after the last key and auto-hidden by 6.5 s. 60/60 tests + lint green | done |
| 2026-08-11 | 5+ | **Split view maximises space: single current page + context half-and-half**. User feedback on the side panel: show only the mushaf page that is CURRENTLY highlighting, and give the context the other half. The split view (tafseer / meanings / translation panel open) is now 50/50: the mushaf half shows the SINGLE page holding the highlighted ayah (picked from the loaded spread by page parity — odd = right side, even = left side) instead of the two-page spread, and the panel owns the other half. New `SingleMushafPage` composable (PageModeView + AnimatedContent crossfade on page change); the panel rows use the full 20 sp text size again (it was 18 sp for the narrower 40% panel). Mushaf-only mode keeps the full two-page spread unchanged. Verified on the emulator (أحمد عيسى المعصراوي • البقرة, Hafs Tajweed): tafseer panel + single page 32 (697×1080, no spine) side by side; the highlight moved down page 32 (y 312→498 for ayah 205, y 642→824 for 208, y 894→938 for 210) and the page crossfaded to 33 when ayah 211 (first of page 33) played, highlight at the new page's top (y 144–172); switching back to مصحف فقط restores the full two-page spread with the spine. 60/60 tests + lint green | done |
| 2026-08-11 | 5+ | **Split view keeps the spine — the mushaf page and the context panel meet around it like an open mushaf**. User feedback: "keep the spine & align the mushaf & tafseer around it as with the 2 page mushaf". The split view (context panel open) is still 50/50 but now laid out as an OPEN MUSHAF: the current highlighted page sits on the RIGHT aligned toward the spine (`SingleMushafPage` gets an `alignment` param → `PageModeView(alignment = CenterEnd)`), the folded `MushafSpine()` ribbon joins it, and the context panel takes the LEFT page's place with its rows starting at the spine (RTL `start = 14dp` inner margin vs `26dp` outer, same for the panel header). The spine + inner-edge shadow were extracted from `MushafSpreadView` into shared composables (`MushafSpine()`, `BoxScope.SpineEdgeShadow(alignment)`) so the mushaf-only mode and the split view share the identical book look. Verified on the emulator (أحمد عيسى المعصراوي • البقرة, Hafs Tajweed): pixel scan shows panel (ends x≈948) · spine ribbon x 951–969 with the darker center crease (106,92,69 / 60,52,40 leather tones) · edge shadow · the highlighted page hugging the spine from x≈990 with the highlight band (ayah 228, x 1008–1614, y 252–870); switching back to مصحف فقط shows the full two-page spread with the same spine at center (x 918–968). 60/60 tests + lint green | done |
| 2026-08-11 | release | **v1.27 released to the samba share** (`/mnt/rpi-share`). Bumped `versionCode` 27→28 / `versionName` "1.26"→"1.27". Carries the three 2026-08-11 features: (1) the mushaf page is always visible with tafseer / word meanings / translation as an OPTIONAL side panel chosen from the top-bar dropdown (صفحة المصحف فقط = full-screen mushaf) + chrome auto-hide 3 s→5 s + empty content rows hidden (2,576 empty word-meaning rows) with nearest-preceding-row pinning; (2) the split view shows only the CURRENT highlighted page half-and-half with the context; (3) the split is laid out as an open mushaf — the highlighted page and the context panel meet around the folded spine (shared `MushafSpine()` / `SpineEdgeShadow`). Pushed as `QuranTV-1.27-debug.apk` (26.7 MB, MD5 477e014918e920178af0327e492c13ea, verified copy); `QuranTV-1.26-debug.apk` kept for rollback. aapt-verified: versionCode 28 / versionName 1.27 | done |
